{
  "name": "githubpro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare": "./scripts/prepare-effect.sh && bun run types",
    "types": "bun node_modules/wxt/bin/wxt.mjs prepare",
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
