/**
 * Compatibility barrel. Visual kit lives in `chrome.tsx`.
 * Keep these names stable — install, welcome, and job pages import them.
 */
export {
  Above,
  AddToChrome,
  Aside,
  Footer,
  HELD,
  INSTALL_AT,
  Nav,
  Press,
  Quietly,
  SkipTo,
  SOURCE_AT,
  Source,
  STORE_AT
} from "./chrome"
